import React from 'react';

const Header = () => (
  <header className="header">
    <h1 className="heading-primary">
      <span className="heading-primary__main">Youtube</span>
      <span className="heading-primary__subtitle">ReactJS</span>
    </h1>
  </header>
);

export default Header;
